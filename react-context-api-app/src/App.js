import './App.css';
import {useState, createContext} from 'react'

const counterContext = createContext(null);
const counterContexProvider = () => {
    
}

function App() {
    return ( <
        div className = "App">
         <header className = "App-header">
           context api 
         </header> 
        </div>
    );
}

export default App;